для корректной работы необходимо создать .env файл
и ввести
"SECRET_KEY={любое слово}" (без кавычек, также недолжно быть специальных символов кроме _)
"PORT={любое число}" (без кавычек)
"DEPLOY=TRUE (or FALSE)" (без кавычек) (true если публикуем на сервер, false, если нет)
LOCAL={ваше локальное значение}


пример:
SECRET_KEY=bug_fixed
PORT=5000
URL=yandexlyc.herokuapp.com
LOCAL=http://192.168.1.164:5000/
DEPLOY=TRUE
